# MkDocs and theme
mkdocs==1.6.1
mkdocs-material==9.5.49

# Plugins
mkdocs-glightbox==0.4.0
mkdocs-git-revision-date-localized-plugin==1.3.0

# Markdown extensions used in mkdocs.yml
pymdown-extensions==10.13

# Pinned for reproducible builds. To upgrade:
# 1. Bump version numbers above
# 2. Test locally with `pip install -r requirements.txt && mkdocs serve`
# 3. Push and verify the GitHub Action build succeeds
